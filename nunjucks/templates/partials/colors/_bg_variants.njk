<!-- _backgrounds_variants.njk -->
		{% if obe_settings['enable-obebs4-background-tints-shades'] === 'true' %}
		<div class="container-fluid pt-5 text-center bg-white text-dark">
		
			<div class="container">

				<div class="row justify-content-center">
					
					<div id="{{ global_strings['colors']['sections'][3]['id'] }}" class="col-12">
					
						{{ macros.page_section_heading(global_strings['colors']['sections'][3]['head'], global_strings['colors']['sections'][3]['subhead'], global_strings['colors']['sections'][3]['descrip']) }}

					</div>

				</div>

				<div class="row justify-content-center">

					{{ macros.bg_variants('white', 'White') }}
					{{ macros.bg_variants('light-gray', 'Light Gray') }}
					{{ macros.bg_variants('gray', 'Gray') }}
					{{ macros.bg_variants('dark-gray', 'Dark Gray') }}
					{{ macros.bg_variants('black', 'Black') }}

					{{ macros.bg_variants('red', 'Red') }}
					{{ macros.bg_variants('rose', 'Rose') }}
					{{ macros.bg_variants('magenta', 'Magenta') }}
					{{ macros.bg_variants('violet', 'Violet') }}
					{{ macros.bg_variants('blue', 'Blue') }}
					{{ macros.bg_variants('azure', 'Azure') }}
					{{ macros.bg_variants('cyan', 'Cyan') }}
					{{ macros.bg_variants('spring-green', 'Spring Green') }}
					{{ macros.bg_variants('green', 'Green') }}
					{{ macros.bg_variants('chartreuse', 'Chartreuse') }}
					{{ macros.bg_variants('yellow', 'Yellow') }}
					{{ macros.bg_variants('orange', 'Orange') }}

					{{ macros.bg_variants('orange-gray', 'Orange Gray') }}
					{{ macros.bg_variants('dark-gray-orange', 'Dark Gray Orange') }}
					{{ macros.bg_variants('blue-gray', 'Blue Gray') }}
					{{ macros.bg_variants('light-gray-azure', 'Light Gray Azure') }}

				</div>

			</div>

		</div>
		{% endif %}