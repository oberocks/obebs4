<!-- _bs_spinners.njk -->
		<div class="container-fluid pt-5 text-center bg-white text-dark">
        
            <div class="container modal-workspace">

                <div class="row justify-content-center pt-5">
                    
                    <div id="{{ global_strings['defaults']['sections'][77]['id'] }}" class="col-12">
                        
                        {{ macros.page_section_heading(global_strings['defaults']['sections'][77]['head'], global_strings['defaults']['sections'][77]['subhead'], global_strings['defaults']['sections'][77]['descrip']) }}

                    </div>

                </div>

                <div class="row justify-content-center">

                    <div class="col-md-6 pb-5">
                        
                        <h5 class="pt-4">Spinner Types</h5>

                        {{ macros.spinner('Border', 'spinner-border') }}
                        {{ macros.spinner('Growing', 'spinner-grow') }}

                        <h5 class="pt-4">Small Spinners</h5>
                        
                        {{ macros.spinner('Small Border', 'spinner-border spinner-border-sm') }}
                        {{ macros.spinner('Small Growing', 'spinner-grow spinner-grow-sm') }}

                        <h5 class="pt-4">Large Spinners</h5>

                        {{ macros.spinner('Large Border', 'spinner-border', 'width: 3rem; height: 3rem;') }}
                        {{ macros.spinner('Large Growing', 'spinner-grow', 'width: 3rem; height: 3rem;') }}

                        <h5 class="pt-4">Spinner Buttons</h5>

                        <label class="d-block">Border Spinner Button</label>
                        <div class="js-prism mb-2">
                            <button class="btn btn-primary" type="button" disabled>
                                <span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>
                                <span class="sr-only">Loading...</span>
                            </button>
                        </div>

                        <label class="d-block">Grow Spinner Button</label>
                        <div class="js-prism mb-2">
                            <button class="btn btn-primary" type="button" disabled>
                                <span class="spinner-grow spinner-grow-sm" role="status" aria-hidden="true"></span>
                                <span class="sr-only">Loading...</span>
                            </button>
                        </div>

                        <label class="d-block">Border Spinner &amp; Text Button</label>
                        <div class="js-prism mb-2">
                            <button class="btn btn-primary" type="button" disabled>
                                <span class="spinner-border spinner-border-sm mr-2" role="status" aria-hidden="true"></span>
                                Loading...
                            </button>
                        </div>

                        <label class="d-block">Grow Spinner &amp; Text Button</label>
                        <div class="js-prism mb-2">
                            <button class="btn btn-primary" type="button" disabled>
                                <span class="spinner-grow spinner-grow-sm mr-2" role="status" aria-hidden="true"></span>
                                Loading...
                            </button>
                        </div>

                        <label class="d-block">Large Border Spinner Button</label>
                        <div class="js-prism mb-2">
                            <button class="btn btn-xl btn-primary" type="button" disabled>
                                <span class="spinner-border" role="status" aria-hidden="true"></span>
                                <span class="sr-only">Loading...</span>
                            </button>
                        </div>

                        <label class="d-block">Large Grow Spinner Button</label>
                        <div class="js-prism mb-2">
                            <button class="btn btn-xl btn-primary" type="button" disabled>
                                <span class="spinner-grow" role="status" aria-hidden="true"></span>
                                <span class="sr-only">Loading...</span>
                            </button>
                        </div>

                    </div>

                    <div class="col-md-6 pb-5">

                        <h5 class="pt-4">Border Spinner Colors</h5>

                        {{ macros.spinner('Primary Border', 'spinner-border text-primary') }}
                        {{ macros.spinner('Secondary Border', 'spinner-border text-secondary') }}
                        {{ macros.spinner('Success Border', 'spinner-border text-success') }}
                        {{ macros.spinner('Danger Border', 'spinner-border text-danger') }}
                        {{ macros.spinner('Warning Border', 'spinner-border text-warning') }}
                        {{ macros.spinner('Info Border', 'spinner-border text-info') }}
                        {{ macros.spinner('Light Border', 'spinner-border text-light') }}
                        {{ macros.spinner('Dark Border', 'spinner-border text-dark') }}

                        <h5 class="pt-4">Grow Spinner Colors</h5>

                        {{ macros.spinner('Primary Grow', 'spinner-grow text-primary') }}
                        {{ macros.spinner('Secondary Grow', 'spinner-grow text-secondary') }}
                        {{ macros.spinner('Success Grow', 'spinner-grow text-success') }}
                        {{ macros.spinner('Danger Grow', 'spinner-grow text-danger') }}
                        {{ macros.spinner('Warning Grow', 'spinner-grow text-warning') }}
                        {{ macros.spinner('Info Grow', 'spinner-grow text-info') }}
                        {{ macros.spinner('Light Grow', 'spinner-grow text-light') }}
                        {{ macros.spinner('Dark Grow', 'spinner-grow text-dark') }}

                    </div>

                </div>

            </div>

        </div>